"""VisuaLex E2E + stress harness. Standalone: does not touch app code.

Run from the repo root:
    python -m e2e.runner --preflight-only
    python -m e2e.runner
    python -m e2e.stress --users 10 --duration 60
"""
